# Copy this file to .env and fill in real values from your Supabase
# project dashboard (Project Settings > API). Never commit the real .env
# file — it's already excluded in .gitignore.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
